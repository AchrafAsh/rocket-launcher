Alfred Project:

> Improvements :
   - fix the extension of the bar (xpad)
   - resize the suggestion frame when it's empty
   - if no prefix is given suggest 'search the web'
   - bring focus by pressing <windows + space>

> To learn :
   - learn to predict research (from previous research) #ML
   - open app on start
   - how to bring focus (unminimize) w/ key event

> Command lines :
  + open a file : start file directory
  + open a folder : start folder directory
  - list of folder content :
	   dir folder_name /b (for the name list only)
	   dir folder_name\*.doc (for specific format)
	   dir folder_name /h (hidden folders and files)